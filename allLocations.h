#pragma once
#include "Location.h"
#include "EnemyBlob.h"

inline QPoint point1(0, 400);
/*inline Location location2(QRect(0, 0, 700, 500), QPixmap(":/sources/background.png"),
                   std::vector<Enemy>({EnemyBlob(point1)}), std::vector<QRect>({QRect(0, 400, 700, 500)}),
                   QPoint(600, 400));*/